"""Development-only tooling. Never imported by `librairy`.

What lives here is for looking at LibrAIry while building it — a fixture
library with one of every finding shape, so a real browser has something
representative to render. `tests/test_dev_tooling.py` asserts that the
production package, image and compose file know nothing about any of it.
"""
